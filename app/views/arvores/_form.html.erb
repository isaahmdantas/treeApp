<%= form_with(model: arvore, local: true) do |form| %>
  <% if arvore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(arvore.errors.count, "error") %> prohibited this arvore from being saved:</h2>

      <ul>
      <% arvore.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
